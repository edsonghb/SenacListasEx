let pi = Number(prompt('Digite o valor do pi = 3.14 '))
let raio = Number(prompt('Digite o valor do raio'))
let areacirculo = ((raio)*(raio))*(pi)
alert(`Área do circulo (${raio}*${raio})*${pi}=${areacirculo}`)